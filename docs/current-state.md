# Pho Code: current state

Last updated: 2026-09-01

## What exists today

Pho Code is a personal macOS-first Electron application built directly on Pi SDK `0.84.4`. It currently has:

- a sandboxed React renderer behind a narrow typed Electron bridge;
- accepted window-first startup: metadata appearance, IPC, and welcome/recents load before a dynamically imported Pi runtime; starting/failed states are explicit and Pi-backed controls stay disabled until ready (desktop verified; packaged behavior and elapsed-time measurements were explicitly not verified at closure);
- local workspace selection and recent-project navigation;
- persistent Pi JSONL sessions with new, list, open, resume, and immediate sidebar state;
- real provider/model discovery plus model and thinking-level selection;
- experimental backend-pinned Codex and Claude ACP sessions selected from the composer; external backends advertise their own model/reasoning/Fast controls, forward text and keyed live-tool deltas, and keep completed snapshots authoritative. Codex/ACP still own their agent loops, built-in tools, persistence, configuration, and authentication. Codex threads additionally receive bounded Pho Code developer instructions and one read-only `pho_search_workspace_references` tool through App Server's experimental dynamic-tool API; Pho Code does not pass Pi's compiled prompt or Pi tool registry across the boundary, and stable ACP v1 has no equivalent client-instruction/tool operation;
- multi-turn streaming chat with sanitized GFM live tokens, compositor-only enter/pulse motion, thinking blocks, KaTeX/Shiki/Mermaid/SVG after settle (KaTeX only when the text looks like math; SVG as a data-URL image with lightbox), copy and owner rewrite for settled assistant output and copy for fenced code blocks, tool activity (collapsed headings are owner-facing titles such as Browse/Run; rows use small quiet preview text; sandboxed bash shows a shield; web search/fetch show colorful site icons without a query/URL chip) grouped into narrative phases headed by the assistant's pre-tool narration prose with terse tool-derived summary labels, live and settled, Stop, and error states;
- a T3-inspired conversation UI with a wider collapsible, mouse-resizable project/session sidebar (manual folder order, no MRU bump on session switch; collapse swaps the panel for a compact overlay pill with Home, Open folder, New session, and Settings, or those same actions in the chat header when the right sidebar is expanded so the conversation fills the left pane; Home returns to the welcome launcher without disposing background sessions; collapse control on the macOS traffic-light inset’s right and leading on Linux), a welcome launcher when no session is live, per-chat activity with a chat icon or 3×3 running mark while a run is live, archive/restore, Move chat to Trash, and a warned Remove project that trashes that folder’s chats, a centered empty-session composer (workspace and local-machine chips) headed by a greeting with round starter-task pills that fill the draft on click or keyboard, and an anchored composer after the first message (greeting, starter chips, and context chips hide); session titles are a short summary (Pi session name, else a humanized first-prompt fallback that never includes expanded skill bodies; after the first settled turn the current model may persist a 3–8 word title); session switch updates sidebar selection and the chat pane only (cached chats paint immediately; uncached opens show chat-pane loading), and live tokens update a transcript tail rather than the settled turn list;
- a persistent right-sidebar icon cluster in the chat header (Context prompt, Plan, Changes, and the unaccepted V5 Task candidate open as floating tiles in a mouse-resizable region; Changes is the review window filling its tile with the same border and glass fill as the other tiles). Task exposes the living Brief, bounded Evidence, source-linked Verification, and authoritative Completion projections and becomes inspect-only during a live run. Context prompt chips show compact character counts; Pi docs is off by default (uncustomized chats strip Pi’s baked docs block); the Windows `powershell` tool is omitted. FileDiff or a write/edit tool card docks Changes; clicking FileDiff again or ⌘R / Ctrl+R hides it (⌘⇧R / Ctrl+Shift+R reloads). The docked region is mouse-resizable (default 520px, up to 1100px or 62% of the window). Approve is ledger state only; conversation stays primary; the review toolbar carries a collapsed `(i)` control stating where snapshots live, that they are not encrypted at rest, and that the 250 MiB ledger budget marks new snapshots unavailable;
- a source-controlled baked-feature manifest that ignores arbitrary global/project extensions, skills, prompts, and themes, plus three Pho Code-authored text-only skills available through `/` and the agent's Skill (`read_skill`) tool after the built-in source (always on) or an explicitly enabled Codex/Cursor/Claude/Pi user root;
- `@gotgenes/pi-permission-system` `24.0.0` as the first baked feature;
- `pi-cursor-sdk` `0.2.0` as a baked Cursor provider (local Cursor SDK agents only; ambient `~/.cursor` settings and Cursor Cloud are disabled by harness policy); Cursor models appear in the picker only after a stored API key or `CURSOR_API_KEY`; selecting a Cursor model shows an honest warning dialog with the Cursor mark; Cursor SDK tools (`cursor_ask_question`, `cursor_activate_skill`, `cursor`) are registered with the Pi session but only active, compiled into the context prompt, and listed in the Context prompt Tools group while that session’s model is Cursor;
- canonical `find` and `grep` tools backed by the bundled FFF native index, plus composer inline `@path` mentions from that same workspace-scoped index; the former additive FFF tool ids and multi-pattern variant are not registered ([accepted evidence](./archive/urgent/2026-08-28-defect-canonical-fff-retrieval.md));
- additive `pho-web` tools `web_search` (parallel DuckDuckGo, Bing, Brave, Mojeek, and Jina, merged unique URLs) and `fetch_content` (bounded public HTTP GET with Readability, YouTube captions/metadata, then Jina Reader for thin JS pages) with SSRF/redirect/size limits;
- Pi-native **Steer current run** / **Add follow-up** commands with pending queue chips projected from Pi;
- image attachments (PNG/JPEG/GIF/WebP) picked or pasted, prepared without absolute paths, and admitted only when the selected model accepts images; a paste/send rejection banner auto-clears after a few seconds;
- an application-owned `move_to_trash` tool that uses the operating-system Trash facility and never falls back to permanent deletion;
- desktop confirm/select/input permission dialogs with Allow once, Allow for this session, and No with an optional reason on the same card, notifications, cancellation, and session rebind;
- an Agent-chat `ask_user_question` questionnaire card and per-chat Plan/Agent toggle (composer mode icon: Bot/ListTree, menu for Agent/Plan and Images…) with a Plan sidebar document plus session `todo` list on the transcript tool row and Plan rail (accepted Plan/Agent); `ask_user_question`, `update_plan_document`, `todo`, and `execute_plan` are permission-allow-listed so they do not open a permission dock on a synced config; Execute is the Plan footer button **or** `execute_plan` after the owner asks in chat; the Execute kickoff is hidden from the transcript; managed profiles allow `web_search` and ask for `fetch_content`;
- typed Settings in a floating Appearance / Accounts / GitHub / Skills / Archived / Permissions / Sandbox dialog for palette + light/dark/system mode (Default, Gruvbox, Catppuccin, Flexoki, GitHub, One Dark), Lucide work-entry icons by default (Pho, CodeX, and colorful Meteocons remain Settings switches; Brand marks default to mono Lobe and can switch to Color), optional frosted-glass blur with strength control, independent UI and chat font sizes, installed UI and code font families (OS faces, not bundled files) with a live code preview, a font-smoothing toggle, built-in and trusted instruction sources with Refresh, a Settings-controlled read-only GitHub MCP row (default off; PAT in the OS secret store), the implemented-but-unaccepted Ask/Auto/Full approval controls with explicit Custom/shared-root migration, reviewer privacy/cost disclosure, redacted on-demand history, honest private/shared data-scope disclosure, captured write/edit snapshots stored in app data (not encrypted at rest in personal v3), a project-permission trust dialog/banner when a workspace override is present, and an active agent-tool boundary (default-on fail-closed Seatbelt wrap for contained agent bash; healthy sandbox skips duplicate bash and in-policy file-tool asks; in-process `read`/`write`/`edit` policy; packaged macOS stages the pinned engine and bundled `rg`);
- in-app provider account login (API key and OAuth) that never returns stored secrets or authorization URLs to the renderer, with compact Settings rows that keep API-key fields collapsed until explicitly opened;
- an unsigned local macOS bundle that stages Pi, the permission feature, the pinned sandbox-runtime engine, and bundled `rg` under app-owned resources;
- internal feature/version diagnostics without install, enable, disable, or marketplace controls;
- bounded shutdown, CSP/navigation/permission guards, context isolation, renderer sandboxing, and disabled Node integration.

## Standalone product direction

The intended product is a standalone harness powered by an embedded, bare-bones Pi runtime. The application—not the user's separate Pi installation—owns its complete capability bundle:

- the exact Pi SDK/runtime version;
- every baked extension and its dependencies;
- every baked skill and prompt;
- every selected MCP adapter/server and required assets;
- host UI adapters, settings adapters, and feature configuration defaults.

The user should install Pho Code and receive those capabilities automatically. They should not need to install Pi packages, copy skills, edit their Pi package settings, configure an MCP adapter separately, or even have a Pi CLI installation for feature loading.

Pho Code now owns its Pi operational data by default under Electron `userData/pi-agent`: provider credentials, model definitions, sessions, permission config, and permission logs. `PHO_CODE_AGENT_DIR` remains an explicit development/interoperability override and is disclosed as shared in Settings. No automatic migration is promised for this pre-release transition.

Milestones 0 through 5 are accepted, and the personal v1 is complete. The owner has also exercised real-provider chat. Signed/notarized public installers and Linux desktop remain outside the verified v1 surface.

## Workstream status

- **V1:** accepted and archived. Its standalone shell, settings, identity, packaging, credential, and permission evidence lives under [`archive/v1`](./archive/v1/README.md).
- **V2:** accepted and archived. Daily-driver autonomy, retrieval, provider accounts, independent sessions, interoperable text-only skills, and the fixed read-only GitHub MCP record live under [`archive/v2`](./archive/v2/README.md).
- **V3 — Change Control and Recovery:** accepted and archived. Tracked Pi `write`/`edit` changes have bounded unified-diff review, Approve, conflict-safe per-file Undo, restart persistence, corrupt-ledger fail-closed behavior, and real macOS Trash recovery. Undo all and shell/MCP mutation recovery remain unavailable. See the [V3 acceptance review](./archive/v3/logs/2026-08-16-v3-acceptance-review.md).
- **V4 — Public Beta Foundation:** promoted 2026-08-20, then marked **Pending** the same day. The owner cannot enroll in the Apple Developer Program, so Developer ID signing, notarization, Gatekeeper-clean DMG, and Homebrew cask distribution cannot be verified. Fail-closed proof packaging remains; Milestone 0's frozen identity/origin constants were removed 2026-08-27 as unreferenced scaffolding ([preflight](./version/v4/logs/2026-08-20-m0-release-preflight.md), [hold](./version/v4/logs/2026-08-20-hold-pending-apple-developer.md)). Remaining V4 milestones are held. Independent add-ons, UI, and promoted V5 may proceed and must not absorb V4's signing, update, diagnostics, or utility-process contracts. None of V4's planned public-beta claims is accepted. See [`version/v4`](./version/v4/README.md).
- **V5 — Pho Agent Foundation:** **Awaiting owner verification; not accepted.** The 2026-08-28 hold was explicitly superseded for grouped M1–M4 implementation by owner direction on 2026-09-01 because no real model is currently available. The backend-neutral host remains in source, and Pi now supplies the first native Task intelligence adapter: an append-only living Task Brief, bounded inspectable evidence pack, source-linked verification ledger, evidence-backed completion, deterministic evaluation, five typed owner commands, two baked tools, and a Task right-sidebar tile. Focused core/product/Electron/package lanes pass, including packaged relaunch with PATH without Pi; the deterministic mechanics cohort passes but makes no real-provider quality claim. Codex/ACP do not advertise Task intelligence. Full repository/desktop matrices retain documented unrelated in-progress failures, and the owner real-model pass remains the acceptance gate. Prior Pi-only M0 acceptance is still reopened; external-backend permission/resume/GUI-PATH/cross-backend acceptance gaps and V4's separate hold remain. See [`version/v5`](./version/v5/README.md), the [Task implementation evidence](./version/v5/logs/2026-09-01-m1-m4-task-intelligence-implementation.md), and the [owner handoff](./version/v5/handoff.md).
- **Integrated terminal add-on:** promoted and in implementation; no PTY or Terminal rail exists in source. Product, plan, and handoff live under [`features/terminal`](./features/terminal/README.md).
- **Context compaction add-on:** promoted and in implementation; Milestones 0–4 are implemented and machine-verified 2026-09-01, pending owner real-model acceptance. Transcript boundaries preserve the full display branch across the cut and survive restart; the usage popover carries Compact context and Cancel; threshold/overflow auto-compaction projects live. The Pho cutover strategy is in source: a banded remaining-context signal, per-session notes tools over a bounded sidecar that follows Archive/Trash, read-only history lookup, a `session_before_compact` hook that substitutes the notes digest for Pi's summary on every trigger (empty notes fall back to Pi), and a two-phase `new_context` tool. Foundational contracts, projection, controller, and the continuity feature live under `packages/pho-agent/`; six characterization tests pin Pi `0.84.4` compaction behavior. Product, plan, research, implementation evidence, and the owner test guide live under [`features/compaction`](./features/compaction/README.md) ([handoff](./features/compaction/handoff.md), [M0–M4 log](./features/compaction/logs/2026-09-01-m0-m4-implementation.md)).
- **Approval modes and automatic review add-on:** promoted 2026-09-01 and in implementation; Pi Milestones 0–3 are implemented and machine-verified, pending real-provider and owner desktop acceptance. The reusable exact-input controller, grants, ordered policy/revalidation pipeline, isolated reviewer lifecycle, circuit breaker, and Pi whole-action interception live in `packages/pho-agent`; Pho Code owns product invariants, boundary/sandbox adapters, typed settings and migration, redacted history, protocol/application/Electron projection, and the composer/Settings/approval UI. Ask and Auto share contained policy; Full is per-chat and process-memory-only, with product invariants still terminal. Legacy Custom/shared permission roots remain Ask-only until explicit migration. External Codex/ACP sessions remain truthfully Ask-only under the unaccepted backend capability boundary. Product, plan, [machine evidence](./features/approval-modes/logs/2026-09-01-m0-m3-pi-implementation.md), and [owner handoff](./features/approval-modes/handoff.md) live under [`features/approval-modes`](./features/approval-modes/README.md).
- **Subagent orchestration add-on:** promoted 2026-09-01 and in implementation; research and a corrected product/implementation contract exist, but no Pho child coordinator, relationship store, `pho_subagent` tool, transcript card, or Agents surface is implemented. The Pho backend is the app-owned Pi wrapper (`pi` in current source protocol), and only it owns Pho orchestration: a Pho/Pi parent creates fresh, named, separately inspectable Pho/Pi children against configured Pho models. The parent can rediscover Stop, steer/follow up, Continue the same child from exploration into a separately authorized writer phase, and explicitly relay bounded context between siblings. Codex/Claude own their native subagents; Pho only renders or explains adapter-proven activity/metadata/controls. Direct peer messages, nesting, workflows, scheduling, profiles, CLI adapters, and worktrees stay out. Product, plan, research, and owner correction live under [`features/subagents`](./features/subagents/README.md).
- **Git change tracking add-on:** promoted 2026-09-01 and in implementation; research and the product/implementation contract exist, but no git evidence row, shadow repository, run checkpoint, or ledger cutover exists in source yet. Accepted V3 change review remains the shipped truth. The add-on adds read-only git working-tree evidence to the Changes surface, then an app-owned per-workspace shadow git repository that checkpoints every run of every backend (Pi, Codex, Claude ACP) so bash, MCP, and external-backend mutations appear as honestly labeled observed entries next to exactly-attributed `write`/`edit` records. The owner's repository stays read-only to Pho; Undo keeps the V3 hash/identity gates with shadow blobs as the byte source; nested repositories are excluded, never manipulated. Product, plan, and research evidence live under [`features/git-change-tracking`](./features/git-change-tracking/README.md); the companion session-switch defect fix is logged in [`ui/logs/2026-09-01-bug-changes-tile-session-switch.md`](./ui/logs/2026-09-01-bug-changes-tile-session-switch.md).
- **Plan / Agent and ask-user add-on:** accepted 2026-08-18; workstream archived 2026-08-18. Agent default; Plan write-tool policy (not a sandbox); juicesharp-style ask-back card; session `todo` in Agent and Plan; Plan document + Execute with V3 capture; no baked juicesharp/`pi-tui`. Composer mode is the Bot/ListTree context button (not a plus, not a labeled chip); Stay/Refine buttons and composer `n/m` chip are not shipped. Todos stay on the transcript tool row and Plan rail. Living behavior is architecture. Product, plan, and review live under [`archive/features/plan-agent`](./archive/features/plan-agent/README.md). Closure: [`archive/features/plan-agent/logs/2026-08-18-workstream-closure.md`](./archive/features/plan-agent/logs/2026-08-18-workstream-closure.md).
- **Agent-tool sandbox add-on:** accepted 2026-08-17; workstream archived 2026-08-18. Default on; fail-closed Seatbelt wrap for agent bash; healthy sandbox skips bash and in-policy `read`/`write`/`edit` asks; extra-workspace writes may still ask on `external_directory`; packaged macOS stages the pinned engine and bundled `rg`. Living behavior is architecture. Product, plan, and review live under [`archive/features/sandbox`](./archive/features/sandbox/README.md). Closure: [`archive/features/sandbox/logs/2026-08-18-workstream-closure.md`](./archive/features/sandbox/logs/2026-08-18-workstream-closure.md). Distinct from V4 runtime crash isolation and from the urgent window-first track.
- **Urgent queue:** the [runtime/renderer decomposition](./urgent/2026-08-27-prerequisite-runtime-and-renderer-decomposition.md) prerequisite remains in implementation. [Agent stop](./archive/urgent/agent-stop/README.md) is accepted and archived 2026-08-20: bounded Stop cancels a selected stuck run/dialog within a 1 s Pi-idle deadline, Stop-all covers working/attention rows across background chats, and controller/test teardown is bounded ([closure](./archive/urgent/agent-stop/logs/2026-08-20-m2-acceptance-and-closure.md)). [Window-first Pi core](./archive/urgent/window-first-pi-core/README.md) is also accepted and archived: metadata chrome loads before dynamic Pi construction ([closure](./archive/urgent/window-first-pi-core/logs/2026-08-20-m1-acceptance-and-closure.md)). Its package assertion and five timing clocks were explicitly waived/deferred, so no packaged or latency claim is made. Pi still shares Electron main; process extraction remains a pending V4 contract. A 2026-08-30 investigation of core load time is recorded in [`urgent/2026-08-30-startup-pi-core-load.md`](./urgent/2026-08-30-startup-pi-core-load.md): the wait is Pi's public library barrel (TUI, jiti, every provider) loaded from `node_modules` after window-first; the pin is now `0.84.4` and does not remove that cost. See [`urgent/README.md`](./urgent/README.md).
- **Conversation UI:** active independent track for transcript, composer, project/session chrome, and shared right-sidebar host behavior under [`ui`](./ui/README.md).

Browser automation, broader shell-mutation recovery, session tree/fork, multi-agent worktrees and integration, Linux distribution, generic memory, Pho Research, and hostile-workspace containment remain outside the promoted V5 scope or unpromoted core research in the [`version` roadmap](./version/roadmap-vnext.md). Bounded separate-session delegation is promoted independently under [`features/subagents`](./features/subagents/README.md). Public-beta distribution and Pi-process extraction are promoted V4 work held pending Apple Developer Program enrollment. Context compaction and V5 Task intelligence are implemented and machine-verified, each with an owner real-model acceptance handoff pending. Git change tracking is promoted under [`features/git-change-tracking`](./features/git-change-tracking/README.md).

## Related records

Use [`archive`](./archive/README.md) for immutable history, [`version`](./version/README.md) for numbered core product versions (V4 pending; V5 awaiting owner verification), [`features`](./features/README.md) for current add-ons, [`urgent`](./urgent/README.md) for owner-priority work to do first, [`ui`](./ui/README.md) for conversation chrome, [`architecture`](./architecture/README.md) for accepted boundaries, and [`development.md`](./development.md) for commands and verification. Update this brief when implemented behavior or workstream status changes.
