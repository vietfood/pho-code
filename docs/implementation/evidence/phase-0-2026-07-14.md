# Phase 0 foundation evidence — 2026-07-14

- Gate result: **PASS**
- Environment: macOS, Apple-native GPUI/Metal build
- Scope: [Phase 0 foundation](../v1/phase-0-foundation.md)

## Delivered foundation

- One Rust package with GPUI and `gpui_platform` pinned to Zed revision `7cf50a771f54427f76b4584030c7b3b66f4e39f5`.
- One application-owned Tokio runtime and centralized queue, byte, concurrency, approval, continuation, and duration limits.
- System-aligned `app`, `auth`, `backend`, `agent`, `tools`, `session`, and `ui` module boundaries.
- Opaque local identities independent from provider IDs.
- Typed intents and runtime events, deterministic reducer, bounded diagnostics, stale-approval rejection, and immutable terminal turns.
- Narrow `ModelBackend` seam and deterministic `ScriptedBackend` without GPUI interaction.
- Crate boundaries for clock, credential, session, artifact, loopback, temporary-workspace, and deterministic-process tests.
- Minimal GPUI startup view that retains the application runtime and OS-released single-instance guard and displays initialization failure in-window.
- Standard-library-only Markdown target/heading validator covering `AGENTS.md` and `docs/**`.
- Reviewed direct dependency purposes, pins, features, and licenses in [the dependency baseline](../dependencies.md).

## Verification run

The following commands ran successfully after the final edits:

```text
cargo fmt -- --check
cargo check --offline
cargo build --offline
cargo test --all-targets --offline
cargo test --test keychain_macos --offline -- --ignored --nocapture
cargo clippy --all-targets --all-features --offline -- -D warnings
cargo tree -e features --offline
cargo tree -d --offline
```

The full automated run passed 33 library tests, 1 documentation-link test, 3 foundation integration tests, and 2 single-instance process tests. The separate opt-in macOS Keychain round-trip/delete component test passed with a unique synthetic development service.

The GPUI commands required permission to use the normal Clang/Metal module cache outside the repository sandbox; the escalated checks then passed. This is an execution-environment constraint, not a skipped verification.

## Dependency review

The active graph contains one Zed/GPUI Git source, one Tokio version, and one Rustls family for the Pho Code HTTP path. GPUI's internal `smol` dependency remains upstream implementation detail; Pho Code creates only its Tokio runtime. `gpui-component` was deferred to Phase 6 because adding its current unqualified Zed dependency would produce a second GPUI source identity.

## Gate assessment

The supported macOS environment builds the GPUI scaffold, the headless scripted target runs without starting GPUI, long-running work is owned outside views, dependencies are reproducible, and current documentation routes to ADR 0002 and the native backend/tools/sessions contracts while historical ADR 0001 material remains explicitly marked as superseded.

Phase 1 is ready and in progress. This result does not claim live OAuth, backend, model, or account compatibility.
